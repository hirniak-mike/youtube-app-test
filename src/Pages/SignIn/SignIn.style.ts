import styled from 'styled-components';

export const SSingIn = styled.main`
  font-family: 'Montserrat Alternates', sans-serif;
  background: ${({ theme }) => theme.backgroundColor};
  flex: 1 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 15px 0;
`