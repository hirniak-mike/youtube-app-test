import styled from 'styled-components';

export const SPlaylistPage = styled.main`
  flex: 1 0 auto;
  padding: 25px 0;
  background-color: ${({ theme }) => theme.backgroundColor};
  transition: background-color ease-out .5s;
`